const request = require('supertest');
const api = require('../../');
const db = require('../database/dbConfig');

describe('----- Server -----', () => {
    describe('ROUTE /', () => {
        describe('GET', () => {

        });

        describe('POST', () => {

        });
    });
    describe('ROUTE /:id', () => {
        describe('GET', () => {

        });

        describe('PUT', () => {

        });

        describe('DELETE', () => {

        });
    });
});