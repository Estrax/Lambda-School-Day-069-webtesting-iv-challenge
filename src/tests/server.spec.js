const request = require('supertest');
const api = require('../../');
const db = require('../database/dbConfig');

describe('----- Server -----', () => {
    describe('ROUTE /', () => {

    });
    describe('ROUTE /:id', () => {

    });
});